# Build products — regenerated by `swift build` and `scripts/build-app.sh`
.build/
build/
Releases/

# Xcode / SwiftPM local state
.swiftpm/xcode/package.xcworkspace/xcuserdata/
*.xcuserdatad/
xcuserdata/
DerivedData/

# Generated at build time from project.yml
Resources/Info-Generated.plist

# Personal photographic data — not redistributable.
# testraw/ holds the owner's own RAW/JPEG captures used as local QA fixtures.
# Evidence/ holds QA screenshots, many of which show those photographs.
# Tests that need fixtures read RAWDESK_SONY_ARW_FIXTURE_DIR and skip when unset.
testraw/
Evidence/

# macOS
.DS_Store
